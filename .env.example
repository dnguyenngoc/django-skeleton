# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database Settings
# For local development with uv (SQLite will be used automatically)
# For Docker (PostgreSQL will be used automatically)
DB_NAME=django_skeleton
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Static Files
STATIC_URL=/static/
STATIC_ROOT=/app/staticfiles/

# Media Files
MEDIA_URL=/media/
MEDIA_ROOT=/app/media/
